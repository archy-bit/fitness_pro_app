import 'package:flutter/material.dart';
class GoalsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Цілі")),
      body: Center(child: Text("Тут будуть цілі")),
    );
  }
}